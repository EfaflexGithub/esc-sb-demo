name: efa_smartconnect_modbus_demo
title: EFA-SmartConnect Modbus Demo
description: This application demonstrates how to use the EFA-SmartConnect Modbus TCP interface.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons:
  get:
  modbus:
  version:
  yaml:
  build:
  logger:
  statemachine:
  intl:
  async:
  data_table_2:
  hive:
  hive_flutter:
  uuid:
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  shared_preferences: ^2.2.0

dev_dependencies:
  test:
  flutter_test:
    sdk: flutter
  flutter_lints:
  hive_generator:
  build_runner: ^2.4.5
  code_builder:
  dart_style:
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true

  assets:
    - LICENSE.md
    - pubspec.yaml
    - assets/icon/icon.png

